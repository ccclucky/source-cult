# Production database connection string.
# Required for persistent data in Vercel/production.
POSTGRES_URL=postgres://<user>:<password>@<host>:<port>/<database>?sslmode=require

# API key used by protected write endpoints (/api/join, /api/activity, etc.).
# Must be a strong random secret in production.
SOURCE_CULT_API_KEY=source-cult-mock-prod-key-change-me

# Chain execution mode: mock | viem
# Set to viem for real Monad on-chain writes.
SOURCE_CULT_CHAIN_MODE=mock

# RPC endpoint used when SOURCE_CULT_CHAIN_MODE=viem.
# Example: Monad RPC URL.
SOURCE_CULT_RPC_URL=

# Private key used by portal signer when SOURCE_CULT_CHAIN_MODE=viem.
# Format: 0x + 64 hex chars.
SOURCE_CULT_PRIVATE_KEY=

# Deployed SourceCult contract address for viem mode.
# Format: 0x + 40 hex chars.
SOURCE_CULT_CONTRACT_ADDRESS=

# Expected chain id for safety check (optional but recommended).
# Example: Monad testnet/mainnet chain id.
SOURCE_CULT_CHAIN_ID=
