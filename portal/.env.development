# Development Environment Configuration
# Loaded when running `npm run dev`
# Secrets (API keys, private keys) go in .env.local

# Database: local SQLite
SOURCE_CULT_DB=./data/sourcecult.db

# Chain Mode: mock for routine dev (no blockchain needed)
SOURCE_CULT_CHAIN_MODE=mock
