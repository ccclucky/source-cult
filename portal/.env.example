# Source Cult Portal — Environment Variables
# ============================================
# Copy this file to .env.local and fill in your values.
#
# File hierarchy (Next.js standard):
#   .env                  — shared non-secret defaults (committed)
#   .env.development      — dev-specific non-secret config (committed)
#   .env.production       — prod-specific non-secret config (committed)
#   .env.local            — ALL secrets, local overrides (NOT committed)
#
# Priority: .env < .env.development/.env.production < .env.local

# ── Secrets (put these in .env.local) ──────────────────────

# API Key for protected endpoints (/api/join, /api/activity, etc.)
SOURCE_CULT_API_KEY=dev-source-cult-key

# PostgreSQL connection string (production only; leave empty for local SQLite)
# POSTGRES_URL=postgres://user:password@host:port/database?sslmode=require

# ── Chain Config (put these in .env.local when using viem mode) ──

# SOURCE_CULT_CHAIN_MODE=viem
# SOURCE_CULT_RPC_URL=http://127.0.0.1:8545
# SOURCE_CULT_PRIVATE_KEY=0x<64-hex-private-key>
# SOURCE_CULT_CONTRACT_ADDRESS=0x<deployed-contract-address>
# SOURCE_CULT_CHAIN_ID=31337
