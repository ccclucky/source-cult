# Database Configuration
# ----------------------
# Local Development (Default): Leave POSTGRES_URL empty to use SQLite
# Production (Vercel): Set POSTGRES_URL to your Vercel Postgres connection string
# POSTGRES_URL=postgres://user:password@host:port/database

# Optional: Custom SQLite path (defaults to ./data/sourcecult.db)
# SOURCE_CULT_DB=./data/my-custom.db

# API Security
# ------------
# API Key for protected endpoints (defaults to 'dev-source-cult-key' in development)
SOURCE_CULT_API_KEY=dev-source-cult-key

# Chain Mode
# ----------
# mock: local deterministic events (default)
# viem: real RPC transactions (required for Monad submission evidence)
# SOURCE_CULT_CHAIN_MODE=mock

# Monad / EVM RPC (required when SOURCE_CULT_CHAIN_MODE=viem)
# SOURCE_CULT_RPC_URL=https://<your-monad-rpc>
# SOURCE_CULT_PRIVATE_KEY=0x<64-hex-private-key>
# SOURCE_CULT_CONTRACT_ADDRESS=0x<deployed-source-cult-contract>
# SOURCE_CULT_CHAIN_ID=10143
